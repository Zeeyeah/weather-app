'use client'

import { observer } from 'mobx-react-lite'
import React from 'react'
import ColorTheme from '../models/ColorTheme'

const BackgroundColors = observer(() => {
  return <div>BackgroundColors</div>
})

export default BackgroundColors
